{
  "name": "web-book",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "build:pages": "cd frontend && npm run build:pages",
    "test": "cd frontend && npm test",
    "preview": "cd frontend && npm run preview"
  }
}
